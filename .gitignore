# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

# Build / temp
*.log
*.tmp
node_modules/
artifacts/

# Local 3D model drops. Keep attribution notes in assets/models/README.md.
assets/models/*.zip
assets/models/*.glb
assets/models/*.gltf
assets/models/*.bin
assets/models/*/
# Exception: the shipped cinematic dragon is redistributable (CC BY 4.0,
# see assets/models/ATTRIBUTION.md) — keep it tracked so the scene works on
# a clone / GitHub Pages. Other model drops stay local.
!assets/models/the_elder_scrolls_blades_ancient_dragon.glb

# Local-only Claude config (project-specific previews)
.claude/launch.json
